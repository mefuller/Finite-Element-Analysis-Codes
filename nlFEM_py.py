import numpy as np

class Mesh:
    """Creates a Mesh object of a rectangular meshed region with arrays containing the nodal positions, element numbering, and the adjoint matrix."""
    def __init__(self, size, num_elements):
        x_length = size[0]
        y_length - size[1]
        num_elements_x = num_elements[0]
        num_elements_y = num_elements[1]
        X = np.linspace()
        self.nodes = np.meshgrid()


def construct_elements_matrices(self)